import {loadListingIdentityIntegrity} from "../lib/listing-identity-integrity-data.ts";import {prisma} from "../lib/prisma.ts";
async function main(){try{const data=await loadListingIdentityIntegrity();console.log(JSON.stringify({store:data.store,evaluated:data.evaluated,counts:data.counts,top50:data.top50.map(row=>({itemId:row.ebayItemId,title:row.title,price:row.currentPrice,classification:row.classification,confidence:row.confidence,player:row.specifics.player,signedBy:row.specifics.signedBy,autographed:row.specifics.autographed,team:row.specifics.team,cardNumber:row.specifics.cardNumber,parallel:row.specifics.parallel,graded:row.specifics.graded,grader:row.specifics.grader,grade:row.specifics.grade,findings:row.findings}))},null,2));}finally{await prisma.$disconnect();}}main().catch(error=>{console.error(error instanceof Error?error.message:String(error));process.exitCode=1;});
