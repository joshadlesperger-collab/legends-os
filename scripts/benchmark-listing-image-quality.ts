import {loadListingImageQuality} from "../lib/listing-image-quality-data.ts";import {prisma} from "../lib/prisma.ts";
async function main(){try{const data=await loadListingImageQuality();console.log(JSON.stringify({generatedAt:data.generatedAt,store:data.store,scanned:data.scanned,imageCounts:{zero:data.counts.zero,one:data.counts.one,two:data.counts.two,threePlus:data.counts.threePlus,average:Number(data.averageImages.toFixed(2)),median:data.medianImages},quality:{highPriority:data.counts.high,review:data.counts.review,healthy:data.counts.healthy,autoCorrectable:data.counts.autoCorrectable,operatorAction:data.counts.operator,duplicateUrls:data.counts.duplicates,brokenUrls:data.counts.broken,lowResolution:data.counts.lowResolution},freshness:data.freshness,limitations:data.limitations,top50:data.top50.map(row=>({itemId:row.ebayItemId,title:row.title,price:row.currentPrice,type:row.listingType,imageCount:row.imageCount,priority:row.priority,actionability:row.actionability,confidence:row.confidence,issues:row.issues}))},null,2));}finally{await prisma.$disconnect();}}main().catch(error=>{console.error(error instanceof Error?error.message:String(error));process.exitCode=1;});
