import type{InventoryHealth}from"@/lib/inventory-health";
export const inventoryActionQueues=[{id:"",label:"Top 15 overall"},{id:"high-value-stale",label:"High-value stale"},{id:"watchers-no-sale",label:"Watchers / no sale"},{id:"stale-no-traffic",label:"Stale + no traffic"},{id:"stale-pricing",label:"Stale + pricing mismatch"},{id:"slow-no-recent",label:"Slow / no recent sales"},{id:"likely-overpriced",label:"Supported price drops"},{id:"potentially-underpriced",label:"Supported price increases"},{id:"fast-seller-low-stock",label:"Fast sellers / low stock"},{id:"high-capital-low-velocity",label:"Stale known capital"},{id:"missing-cost",label:"Missing cost"}]as const;
export function matchesInventoryActionQueue(row:InventoryHealth,issue:string){const stale=row.units90===0&&(row.ageDays??0)>90;if(!issue)return true;if(issue==="high-value-stale")return stale&&row.listedExposure>=50;if(issue==="watchers-no-sale")return(row.watchers??0)>=3&&row.units90===0;if(issue==="stale-no-traffic")return stale&&(row.viewsForDecision??0)<5;if(issue==="stale-pricing")return stale&&row.supportedPriceAction!=null;if(issue==="slow-no-recent")return row.units30===0&&(row.ageDays??0)>=30;return row.issue===issue;}
export function compareQueuePriority(issue:string){return(a:InventoryHealth,b:InventoryHealth)=>issue==="high-value-stale"?b.listedExposure-a.listedExposure||b.priorityScore-a.priorityScore:issue==="watchers-no-sale"?(b.watchers??0)-(a.watchers??0)||b.priorityScore-a.priorityScore:b.priorityScore-a.priorityScore;}
