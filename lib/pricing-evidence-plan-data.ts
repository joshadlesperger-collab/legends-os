import {prisma}from"./prisma";import{buildExternalResearchDryRun,buildInternalComparableIndex,providerRequestBudget,scorePricingEvidencePriority,type InternalSaleEvidence}from"./pricing-evidence-acquisition";
function cachedConfidence(value:unknown){if(!value||typeof value!=="object")return null;const cache=(((value as Record<string,unknown>).compValidation as Record<string,unknown>|undefined)?.cache);if(!cache||typeof cache!=="object")return null;let best:number|null=null;for(const entry of Object.values(cache as Record<string,unknown>)){if(!entry||typeof entry!=="object")continue;const row=entry as Record<string,unknown>,result=row.result as Record<string,unknown>|undefined,confidence=Number(result?.confidenceScore),count=Number(result?.acceptedCompCount),updated=Date.parse(String(row.updatedAt??""));if(Date.now()-updated<=45*86_400_000&&confidence>=60&&count>=3)best=Math.max(best??0,confidence);}return best;}
export async function loadPricingEvidencePlan(){const[listings,saleRows]=await Promise.all([prisma.listing.findMany({where:{listingStatus:"active",recommendations:{none:{status:"pending",type:{in:["raise-price","lower-price"]},suggestedPrice:{not:null},confidence:{gte:60}}}},select:{id:true,title:true,currentPrice:true,quantity:true,startTime:true,views:true,watchers:true,listingQuality:true,authoritativeObservedAt:true,saleEvents:{where:{status:{not:"cancelled"},soldAt:{gte:new Date(Date.now()-90*86_400_000)}},select:{quantity:true}}}}),prisma.saleEvent.findMany({where:{listingId:{not:null},status:{not:"cancelled"},currency:"USD"},select:{id:true,price:true,currency:true,status:true,soldAt:true,orderLine:{select:{title:true}}}})]);const sales:InternalSaleEvidence[]=saleRows.filter(row=>row.orderLine).map(row=>({id:row.id,title:row.orderLine!.title,soldAt:row.soldAt,unitPrice:Number(row.price),currency:row.currency??"",status:row.status})),index=buildInternalComparableIndex(sales);const priorities=listings.map(row=>scorePricingEvidencePriority({listingId:row.id,title:row.title,currentPrice:Number(row.currentPrice),quantity:row.quantity,ageDays:row.startTime?Math.floor((Date.now()-row.startTime.getTime())/86_400_000):null,views:row.views,watchers:row.watchers,units90:row.saleEvents.reduce((sum,sale)=>sum+sale.quantity,0),cachedEvidenceConfidence:cachedConfidence(row.listingQuality),internalComparableCount:index.count(row.title),authoritativeDetails:row.authoritativeObservedAt!=null})).sort((a,b)=>b.score-a.score||a.listingId.localeCompare(b.listingId));const cohortMap=new Map<string,number>(),sourceMap=new Map<string,number>();for(const row of priorities){cohortMap.set(row.cohort,(cohortMap.get(row.cohort)??0)+1);sourceMap.set(row.expectedSource,(sourceMap.get(row.expectedSource)??0)+1);}return{linkedHistoricalSales:sales.length,cohorts:Array.from(cohortMap,([cohort,count])=>({cohort,count})),sources:Array.from(sourceMap,([source,count])=>({source,count})),dryRun:buildExternalResearchDryRun(priorities,providerRequestBudget())};}
