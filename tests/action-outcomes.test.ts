import test from "node:test";import assert from "node:assert/strict";import {buildObservedOutcome,outcomeDueAt} from "../lib/action-outcome-domain.ts";
test("outcome windows are deterministic and do not observe future sales",()=>{const decidedAt=new Date("2026-01-01T00:00:00Z");assert.equal(outcomeDueAt(decidedAt,30).toISOString(),"2026-01-31T00:00:00.000Z");const result=buildObservedOutcome({decidedAt,windowDays:30,evidence:{views:10,watchers:2},currentViews:18,currentWatchers:3,observedAt:new Date("2026-02-01T00:00:00Z"),sales:[{soldAt:new Date("2026-01-12T00:00:00Z"),price:32.95,quantity:1},{soldAt:new Date("2026-02-02T00:00:00Z"),price:40,quantity:1}]});assert.equal(result.saleOccurred,true);assert.equal(result.timeToSaleDays,11);assert.equal(result.quantitySold,1);assert.equal(result.viewsChange,8);assert.equal(result.knownMargin,null);});
test("no authoritative sale is recorded honestly as no sale",()=>{const decidedAt=new Date("2026-01-01T00:00:00Z");const result=buildObservedOutcome({decidedAt,windowDays:60,evidence:{views:null,watchers:null},currentViews:5,currentWatchers:0,observedAt:new Date("2026-03-05T00:00:00Z"),sales:[]});assert.equal(result.saleOccurred,false);assert.equal(result.salePrice,null);assert.equal(result.viewsChange,null);});
