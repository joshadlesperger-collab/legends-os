import test from "node:test";import assert from "node:assert/strict";import {scoreListingRefresh} from "../lib/listing-refresh-agent.ts";
const now=new Date("2026-08-23T00:00:00Z");const row=(overrides:Record<string,unknown>={})=>scoreListingRefresh({listingId:"l",ebayItemId:"1",title:"2024 Topps Card #1",currentPrice:20,listingStatus:"active",listingFormat:"FixedPriceItem",startTime:new Date("2026-04-01T00:00:00Z"),authoritativeObservedAt:new Date("2026-08-22T00:00:00Z"),authoritativeSource:"ebay-trading-get-item",categoryId:"261328",quantity:1,units30:0,units90:0,views30:2,trafficObservedAt:new Date("2026-08-01T00:00:00Z"),watchers:0,promotionStatus:null,currentAdRate:null,negotiationEligible:null,lastPriceChangeAt:null,lastTitleChangeAt:null,lastRefreshAt:null,activeIntervention:false,now,...overrides});
test("mature no-sale inventory remains review when active-offer absence cannot be proven",()=>{const x=row();assert.equal(x.classification,"REVIEW");assert.equal(x.proposedAction,"END → SELL SIMILAR");assert.match(x.evidenceGaps.join(" "),/seller-offer/)});
test("listings younger than 60 days are do not touch",()=>assert.equal(row({startTime:new Date("2026-08-01T00:00:00Z")}).classification,"DO NOT TOUCH"));
test("recent authoritative sales leave the listing alone",()=>assert.equal(row({units30:1}).classification,"LEAVE ALONE"));
test("recent title optimization blocks refresh",()=>assert.equal(row({lastTitleChangeAt:new Date("2026-08-20T00:00:00Z")}).classification,"DO NOT TOUCH"));
test("strong authoritative engagement leaves the listing alone",()=>assert.equal(row({watchers:4}).classification,"LEAVE ALONE"));
test("stale provider state is do not touch",()=>assert.equal(row({authoritativeObservedAt:new Date("2026-07-01T00:00:00Z")}).classification,"DO NOT TOUCH"));
