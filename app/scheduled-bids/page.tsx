import CancelScheduledBidButton from "@/components/CancelScheduledBidButton";
import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
const ct=(date:Date)=>new Intl.DateTimeFormat("en-US",{year:"numeric",month:"numeric",day:"numeric",hour:"numeric",minute:"2-digit",second:"2-digit",timeZone:"America/Chicago",timeZoneName:"short"}).format(date);
export default async function ScheduledBidsPage(){const bids=await prisma.scheduledBid.findMany({orderBy:{scheduledExecutionAt:"asc"},include:{events:{orderBy:{occurredAt:"desc"},take:1}}});return <main className="page"><header className="page-header"><div><div className="eyebrow">Sandbox only</div><h1>Scheduled Bids</h1><p>Durable proxy-bid intents. Live execution remains blocked until a developer-owned Sandbox buyer completes OAuth.</p></div></header><div className="card-warning"><strong>BLOCKED — eBay Sandbox test-user registration.</strong> No Production credentials or bidding endpoints are used.</div><section className="panel"><table><thead><tr><th>Listing</th><th>Operator / recommended max</th><th>Ends / executes</th><th>Status</th><th>Result</th><th>Action</th></tr></thead><tbody>{bids.map(bid=><tr key={bid.id}><td><strong>{bid.listingTitle}</strong><div className="listing-meta">{bid.browseItemId} · {bid.environment}</div></td><td>${Number(bid.operatorMaxBid).toFixed(2)} / {bid.recommendedMaxBid?`$${Number(bid.recommendedMaxBid).toFixed(2)}`:"—"}<div className="listing-meta">Offset {bid.snipeOffsetSeconds}s</div></td><td>{ct(bid.auctionEndAt)}<div className="listing-meta">Execute {ct(bid.scheduledExecutionAt)}</div></td><td><span className="badge badge-neutral">{bid.status}</span><div className="listing-meta">Attempts {bid.attemptCount}</div></td><td>{bid.failureReason??(bid.submittedAt?`Submitted ${ct(bid.submittedAt)}`:"—")}</td><td><CancelScheduledBidButton id={bid.id} disabled={!['DRAFT','SCHEDULED','ARMED'].includes(bid.status)}/></td></tr>)}</tbody></table>{!bids.length?<div className="empty-state">No Sandbox bids scheduled.</div>:null}</section></main>;}
