import CostBasisCsvImport from "@/components/CostBasisCsvImport";
export default function CostBasisImportPage() { return <main className="page"><div className="eyebrow">Profitability readiness</div><h1>Bulk cost-basis import</h1><p>Upload CSV, inspect every match and conflict, then explicitly apply only matched rows. Existing costs change only after Apply.</p><div className="card-info" style={{marginBottom:18}}><strong>Accepted columns</strong><pre style={{overflowX:"auto",color:"var(--text-secondary)"}}>listing_id,ebay_item_id,sku,acquisition_cost,grading_cost,supplies,postage,other_cost,notes</pre></div><section className="panel"><CostBasisCsvImport/></section></main>; }
