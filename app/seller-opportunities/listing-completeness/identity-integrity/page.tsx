import Link from "next/link";import {loadListingIdentityIntegrity} from "@/lib/listing-identity-integrity-data";
export const dynamic="force-dynamic";const Metric=({label,value}:{label:string;value:number})=><div className="metric-card"><span>{label}</span><strong>{value.toLocaleString()}</strong></div>;
const values=(items:string[])=>items.join("; ")||"—";
export default async function ListingIdentityIntegrityPage(){const data=await loadListingIdentityIntegrity();return <main className="page listing-completeness-page"><header className="health-hero"><div><div className="eyebrow">SELL · Listing Optimization · read-only</div><h1>Listing Identity Integrity</h1><p className="health-intro">Deterministic conflicts in current seller-provided item specifics, with Signed By mismatches first. Missing optional fields are not defects and no eBay writes are exposed.</p><p><Link href="/seller-opportunities/listing-completeness">← Listing Completeness</Link></p></div><div className="health-score"><strong>{data.evaluated.toLocaleString()}</strong><span> active</span><small>{data.store?.ebaySellerUsername??"No connected store"}</small></div></header><section className="metric-grid"><Metric label="Signed By populated" value={data.counts.signedByPopulated}/><Metric label="Signed By high" value={data.counts.signedByHigh}/><Metric label="Signed By review" value={data.counts.signedByReview}/><Metric label="Auto No + signer" value={data.counts.autographedNoSignedBy}/><Metric label="High priority" value={data.counts.high}/><Metric label="Review" value={data.counts.review}/><Metric label="Healthy" value={data.counts.healthy}/></section><section className="panel completeness-summary"><div><h2>Conflict families</h2><p><b>Title / player</b><span>{data.counts.titlePlayer}</span></p><p><b>Team</b><span>{data.counts.team}</span></p><p><b>Card number</b><span>{data.counts.cardNumber}</span></p><p><b>Parallel / serial</b><span>{data.counts.parallelSerial}</span></p><p><b>Grading</b><span>{data.counts.grading}</span></p></div><div><h2>Correction boundary</h2><p>Future ItemSpecifics revisions must reconstruct and preserve the complete live provider collection, replacing only the intended logical value.</p></div></section><section className="panel health-section"><div className="eyebrow">Signed By conflicts first</div><h2>Top 50 identity-integrity findings</h2><div style={{overflowX:"auto"}}><table className="completeness-table"><thead><tr><th>Listing</th><th>Identity specifics</th><th>Exact conflict</th><th>Evidence / correction</th></tr></thead><tbody>{data.top50.map(row=><tr key={row.listingId}><td><b><a href={`https://www.ebay.com/itm/${row.ebayItemId}`} target="_blank" rel="noreferrer">{row.title}</a></b><small>{row.ebayItemId} · ${row.currentPrice.toFixed(2)}</small><span className={`badge ${row.classification.startsWith("HIGH")?"badge-danger":"badge-warning"}`}>{row.classification}</span></td><td><small>Player: {values(row.specifics.player)}</small><small>Signed By: {values(row.specifics.signedBy)}</small><small>Autographed: {values(row.specifics.autographed)}</small><small>Team: {values(row.specifics.team)}</small><small>Card #: {values(row.specifics.cardNumber)}</small><small>Parallel: {values(row.specifics.parallel)}</small><small>Graded / grader / grade: {values(row.specifics.graded)} / {values(row.specifics.grader)} / {values(row.specifics.grade)}</small></td><td>{row.findings.map(finding=><small key={`${finding.family}-${finding.conflict}`}><b>{finding.family}</b>: {finding.conflict} ({finding.confidence}%)</small>)}</td><td>{row.findings.map(finding=><div key={`${finding.conflict}-evidence`}><small>{finding.evidence.join(" · ")}</small><small><b>Recommended:</b> {finding.recommendedCorrection}</small></div>)}</td></tr>)}</tbody></table></div>{!data.top50.length&&<div className="empty-state">No deterministic identity conflict was found.</div>}</section></main>}
