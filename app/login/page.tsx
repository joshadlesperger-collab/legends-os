export default function LoginPage({searchParams}:{searchParams?:{error?:string;configuration?:string;next?:string}}){
  return <main className="page-shell" style={{maxWidth:520}}><section className="panel"><div className="eyebrow">Secure operator access</div><h1>Sign in to Legends</h1><p>Business decisions and inventory intelligence are private to the operator.</p>{searchParams?.configuration&&<div className="empty-state">Operator authentication must be configured before sign-in is available.</div>}{searchParams?.error&&<div className="empty-state">That password was not recognized.</div>}<form method="post" action="/api/auth/login" style={{display:"grid",gap:12}}><input type="hidden" name="next" value={searchParams?.next||"/today"}/><label>Operator password<input name="password" type="password" autoComplete="current-password" required style={{display:"block",width:"100%",marginTop:6}}/></label><button type="submit">Sign in securely</button></form></section></main>;
}
